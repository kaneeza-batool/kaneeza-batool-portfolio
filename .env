VITE_GITHUB_USERNAME=kaneeza-batool
# Add your token in .env.local (auto-gitignored) for higher rate limits and GraphQL access:
# VITE_GITHUB_TOKEN=ghp_your_token_here

# ── EmailJS (Contact Form) ──────────────────────────────────────────────────
# Create a free account at https://www.emailjs.com/
# Add VITE_EMAILJS_SERVICE_ID, VITE_EMAILJS_TEMPLATE_ID, VITE_EMAILJS_PUBLIC_KEY
# in .env.local (auto-gitignored) — never commit real keys here.
# See .env.example for the full reference.
