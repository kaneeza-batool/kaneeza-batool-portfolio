import { SectionHeading } from '@components/common'
import SectionWrapper from '@components/layout/SectionWrapper'

function Achievements() {
  return (
    <SectionWrapper id="achievements">
      <SectionHeading
        subtitle="Milestones"
        title="Achievements"
        description="Highlights, awards, and meaningful milestones along the way."
      />
      {/* Achievements content — added in a future prompt */}
    </SectionWrapper>
  )
}

export default Achievements
