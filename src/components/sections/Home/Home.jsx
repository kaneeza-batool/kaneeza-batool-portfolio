import { Container } from '@components/common'
import SectionWrapper from '@components/layout/SectionWrapper'

function Home() {
  return (
    <SectionWrapper
      id="home"
      noContainer
      className="min-h-screen flex items-center justify-center pt-[72px]"
    >
      <Container>
        {/* Hero content — added in next prompt */}
      </Container>
    </SectionWrapper>
  )
}

export default Home
