import { SectionHeading } from '@components/common'
import SectionWrapper from '@components/layout/SectionWrapper'

function Contact() {
  return (
    <SectionWrapper id="contact">
      <SectionHeading
        subtitle="Get in Touch"
        title="Contact"
        description="Have an opportunity or just want to say hello? I would love to hear from you."
      />
      {/* Contact content — added in a future prompt */}
    </SectionWrapper>
  )
}

export default Contact
