import { SectionHeading } from '@components/common'
import SectionWrapper from '@components/layout/SectionWrapper'

function Experience() {
  return (
    <SectionWrapper id="experience">
      <SectionHeading
        subtitle="Work History"
        title="Experience"
        description="My professional journey and the projects I have contributed to."
      />
      {/* Experience content — added in a future prompt */}
    </SectionWrapper>
  )
}

export default Experience
