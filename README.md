# Kaneeza Batool — Portfolio
Built with React + Vite + TypeScript + Tailwind + Framer Motion.

## Setup
npm install
npm run dev

## Contact Form
Uses EmailJS. Add your credentials to src/components/Contact.tsx before deploying.
